# Agent Rules for ComposeCatalog

- Work only within this repository: `/Users/quokkaman/github/ComposeCatalog`.
- For every completed ComposeCatalog task, create a git commit and push it to the remote before reporting completion.
- Do not leave completed work only in the working tree unless the user explicitly asks not to commit or push.
- Before committing, review the diff and run a practical verification step appropriate to the change when possible.
- Do not commit local machine files, secrets, caches, build outputs, or generated artifacts that should stay untracked.
